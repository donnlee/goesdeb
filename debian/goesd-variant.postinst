#!/bin/sh
set -e

pkg=@PKG@
variant=@VARIANT@

priority=10
if [ "$variant" = "example" ] ; then
	priority=1
fi

update-alternatives --install /usr/sbin/goesd goesd /usr/sbin/$pkg $priority

#DEBHELPER#

exit 0
